function rollDice() {
  var user = prompt("Do you want to roll the dice?").toLowerCase();
  if (user === "yes") {
    var dice1 = Math.floor(Math.random() * 6) + 1;
    var dice2 = Math.floor(Math.random() * 6) + 1;
    var result = dice1 + dice2;
    console.log(dice1);
    console.log(dice2);
    console.log(result);
    if (result < 6) {
      console.log("You can do it better!!");
    } else {
      console.log("You're going places!!");
    }
  } else {
    return;
  }
}

rollDice();
